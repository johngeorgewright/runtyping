import { array, lazy, literal, object, optional, type output, undefined as Undefined, union } from 'zod';
import { type $ZodType } from 'zod/v4/core';
import { type Student as _Student, type Teacher as _Teacher } from '@runtyping/test-type-writers/fixtures/source/circular-references';

// This file is generated by runtyping (https://github.com/johngeorgewright/runtyping).
// Manual changes might be lost - proceed with caution!
export const Teacher: $ZodType<_Teacher> = lazy(() => object({ type: literal("teacher"), students: optional(union([array(Student), Undefined(),])), reportsTo: optional(union([Teacher, Undefined(),])), }));

export type Teacher = output<typeof Teacher>;

export const Student: $ZodType<_Student> = lazy(() => object({ type: literal("student"), teacher: Teacher, }));

export type Student = output<typeof Student>;

export const User = union([Student, Teacher,]);

export type User = output<typeof User>;
